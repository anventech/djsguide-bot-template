/* Uso de la plantilla:
module.exports = {
	name: '', //Nombre del evento.
	once: false, //Si el evento se usará una sola vez o se ejecutará varias veces. (true/false)
	execute(client) { //Argumentos del evento, luego de client, separados por comas.
        //Ejecución del evento.
	},
};
*/

//Copiar esto de abajo y pegarlo en su archivo de evento.

module.exports = {
	name: '',
	once: false,
	execute(client) {

	},
};